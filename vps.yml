- hosts:
    - localhost
  remote_user: '{{ remote_user }}'
  roles:
    - base
    - kopia
    - tailscale
    - role: docker
      docker_mtu: 1280
      swarm_advertise_addr: tailscale0
    - swarm
  vars_files:
    - vaulted_vars/vault.yml
    - vaulted_vars/vars.yml
